import openpathsampling as paths

class EnsembleRepresentationAnalysis(object):
    """Check how flat the sampling of ensembles is.

    If your move scheme is designed, but not guaranteed, to give flat
    sampling (e.g., SRTIS), it can be useful to see how the sampling
    frequency evolves.
    """

    def __init__(self, ensemble_trace, labels):
        pass

    def __add__(self, other):
        pass

    def plot(self):
        pass
